# Auto-generated by SCIRun superbuild
# Provides an imported target Tny::tny (STATIC)

if(NOT TARGET Tny::tny)
  add_library(Tny::tny STATIC IMPORTED)
  set_target_properties(Tny::tny PROPERTIES
    IMPORTED_LOCATION             "@TNY_LIB@"      # absolute path to tny(.lib/.a)
    INTERFACE_INCLUDE_DIRECTORIES "@TNY_INC@"      # absolute path to include root (contains tny/tny.h)
  )
endif()

# Legacy variables for consumers that don't use targets
set(Tny_FOUND TRUE)
set(TNY_INCLUDE_DIR "@TNY_INC@")
set(TNY_LIBRARY "@TNY_LIB@")
set(TNY_LIBRARIES "@TNY_LIB@")